<h2> Hi <%= @user.name.capitalize %></h2>
<h3>To reset your password, click the below link</h3>
<br>
<p><%= link_to nil, edit_password_reset_url(@user.password_reset_token) %></p>
<br>
<p> The above link is valid for <strong><%= ENV["RESET_MAIL_VALIDITY_HOURS"] %>hrs</strong> after it its generation, please request again if link expired.</p> 

<p>Please ignore if not requested </p>
<br>
<h4>Thanks,</h4>
<h4>The Flash Team</h4>
publishStateDiv = document.getElementById("publish-data")

<% if @status %>
  publishStateDiv.innerHTML = "<strong > The Deal is Publishable </strong>"
<% else %>
  publishStateDiv.innerHTML = "<strong > The Deal is NOT publishable </strong >"
<%end%>
